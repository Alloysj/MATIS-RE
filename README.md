
  # MATIS Sacco

  This is a code bundle for MATIS Sacco.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  


git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Alloysj/MATIS-RE.git
git push -u origin main